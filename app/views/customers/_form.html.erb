<div class="col-sm-3 col-md-3"></div>
<div class="col-sm-6 col-md-6">

<%= form_for(@customer) do |f| %>

  <% if @customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customer.errors.count, "error") %> prohibited this customer from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %> <br/>
    <%= f.text_field :name, class: 'form-control' %>
  </div>

  <div class="field">
    <%= f.label :slug, 'URL Route/Name' %> <br/>
    <%= f.text_field :slug, class: 'form-control' %>
  </div>  

  <h3>Click to Edit Colors</h3>

  <div class="field">
    <%= f.label :color_one, 'Background Color' %>: <%= f.text_field :color_one, class: 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :color_two, 'Outline Color' %>: <%= f.text_field :color_two, class: 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :color_three, 'Text Color' %>: <%= f.text_field :color_three, class: 'form-control'  %>
  </div>

  <div class="actions">
    <br/>
    <%= f.submit class: 'btn btn-primary' %>
  </div>

<% end %>
</div>

<div class="col-sm-3 col-md-3"></div>

<script>

  var settings = {
    // control: $(this).attr('data-control') || 'hue',
    // defaultValue: $(this).attr('data-defaultValue') || '',
    // inline: $(this).attr('data-inline') === 'true',
    // letterCase: $(this).attr('data-letterCase') || 'lowercase',
    // opacity: $(this).attr('data-opacity'),
    // position: $(this).attr('data-position') || 'bottom left',
    // change: function(hex, opacity) {
    //   if( !hex ) return;
    //   if( opacity ) hex += ', ' + opacity;
    //   try {
    //     console.log(hex);
    //   } catch(e) {}
    //   },
    theme: 'bootstrap'
  };

  $('#customer_color_one').minicolors(settings);
  $('#customer_color_two').minicolors(settings);
  $('#customer_color_three').minicolors(settings);


  // $('#customer_color_one').minicolors();
    // $('.simple_color_custom_chooser_css').simpleColor({ chooserCSS: { 'background-color': 'black', 'opacity': '0.9' } });    
    // $('#customer_color_one').simpleColor();
    // $('#customer_color_two').simpleColor();
    // $('#customer_color_three').simpleColor();
    // $('.simple_color_one').simpleColor({ chooserCSS: { 'background-color': 'black', 'opacity': '0.9' } });    
</script>